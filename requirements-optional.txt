# Optional dependencies for advanced features
# These require C++ build tools to install

# Vector database and embeddings for production RAG
chromadb==0.4.22
sentence-transformers==2.3.1

# To install these, first update Dockerfile to include build tools:
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     build-essential \
#     g++ \
#     && rm -rf /var/lib/apt/lists/*
#
# Then install: pip install -r requirements-optional.txt

