services:
  # Just Ollama - simple and minimal
  ollama:
    image: ollama/ollama:latest
    container_name: simple-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:

